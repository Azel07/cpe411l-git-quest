# Level 1 — The Door Handle 🔑

You can’t escape without reading the rules.

## Task
1) Create a file named `answers.md` at the root of the repo.
2) In `answers.md`, write this exact line:

`LEVEL 1 COMPLETE: I can read instructions`

3) Save the file.

## Commit & Push
```bash
git status
git add answers.md
git commit -m "Level 1: created answers.md"
git push -u origin student-<yourname>